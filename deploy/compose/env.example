# Copy to .env, mode 0600.
DOMAIN=calendar.example.org
OWNER=your-github-user
VERSION=latest

ANYTYPE_API_KEY=
# openssl rand -base64 24
CALDAV_PASSWORD=
# openssl rand -hex 32; keep it, every person's password is derived from it
ACCOUNTS_SECRET=
