# Only the CalDAV facade and push are reachable from outside. The
# iCalendar feed needs no password, so it is not routed here.
{$DOMAIN} {
	redir /.well-known/caldav /dav/ 301
	@server path /dav /dav/* /push/* /healthz
	reverse_proxy @server server:8080
	respond 404
}
